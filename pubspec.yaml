name: ofx_parser
description: A powerful and robust parser for Open Financial Exchange (OFX) and QFX files in Dart. Converts OFX/QFX data into strongly-typed Dart classes with timezone handling.
version: 1.0.0
repository: https://github.com/jarodsim/ofx

environment:
  sdk: ^3.1.0

dependencies:
  xml2json: ^6.2.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.21.0
